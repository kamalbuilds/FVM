import { Default } from 'components/layouts/Default';
import { StorageProviders } from 'components/modules/StorageProviders/'

const TransactionsPage = () => {
  return (
    <Default pageName="StorageProviders">
      <StorageProviders />
    </Default>
  );
};

export default TransactionsPage;
